requests
brotli            # decompress `Content-Encoding: br` responses (some ports use it)
pandas
lxml
beautifulsoup4
html5lib
openpyxl
xlrd>=2.0.1
Pillow
ddddocr
selenium>=4.11.0
supabase
python-calamine>=0.2.0
python-dotenv
schedule
websockets
shapely
pdfplumber
# pdf2image and pytesseract are NOT listed here — they are opt-in for the
# --enable-ocr-fallback flag in subsidy_diesel_sync.py and require a system
# Tesseract binary. Install them manually if needed: pip install pdf2image pytesseract

# ── Client Alerts (scripts/client_alerts/) ────────────────────────────────────
jinja2>=3.1          # email template rendering (immediate + digest)
# Active email backend = Gmail SMTP + App Password (gmail_client.py), which uses
# only the Python stdlib (smtplib / ssl / email) — NO new dependency needed.
# The google-* libs below are now DORMANT for client_alerts (the OAuth Gmail API
# path was retired because its refresh token kept expiring). Kept harmless because
# the legacy alertas/notificador.py still imports them; do not remove blindly.
google-auth-oauthlib
google-api-python-client
google-auth-httplib2
# resend SDK not used — the dormant resend_client.py calls the Resend REST API
# directly via `requests` (kept for a future verified-domain switch).
