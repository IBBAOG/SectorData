requests
brotli            # decompress `Content-Encoding: br` responses (some ports use it)
pandas
lxml
beautifulsoup4
html5lib
openpyxl
xlrd>=2.0.1
Pillow
ddddocr
selenium>=4.11.0
supabase
python-calamine>=0.2.0
python-dotenv
schedule
websockets
shapely
pdfplumber
# pdf2image and pytesseract are NOT listed here — they are opt-in for the
# --enable-ocr-fallback flag in subsidy_diesel_sync.py and require a system
# Tesseract binary. Install them manually if needed: pip install pdf2image pytesseract

# ── Alerts Product (scripts/alerts/) ──────────────────────────────────────────
jinja2>=3.1          # email template rendering
# resend SDK not used — we call the Resend REST API directly via `requests`
# (avoids SDK version coupling; requests is already listed above)
